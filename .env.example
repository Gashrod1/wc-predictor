API_FOOTBALL_KEY=     # optional — freemium 100 req/day, register at api-football.com
FOOTBALL_DATA_KEY=    # optional — free, register at football-data.org
# StatsBomb Open Data and the fallback CSV require no API key

WANDB_API_KEY=        # optional — weights & biases, register at wandb.ai
WANDB_PROJECT=worldcup-predictor
